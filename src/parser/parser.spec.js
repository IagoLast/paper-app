import parser from './parser.js';

describe('parser', () => {
    describe('.render(textInput)', () => {
        xit('should return the right html output from the given text', () => {
            const actual = parser.render(demo);
            expect(actual).toEqual(``);
        });

        //TODO: Add a test case for each parser feature.
    })
})